import React from 'react';
import styled from 'styled-components';

const InfoSection = () => (
  <Section>
    <h2>Why Choose Us?</h2>
    <p>We provide expert guidance, support, and resources for students.</p>
  </Section>
);

const Section = styled.section`
  padding: 4rem 2rem;
  text-align: center;
`;

export default InfoSection;
